<%=
  def validate_json(key)
      JSON.parse(key)
    rescue JSON::ParserError => e
      raise "Invalid gcp_service_account_key provided; it is not valid JSON"
  end

  if p('enabled')
    key = p('gcp_service_account_key')
    validate_json(key)
    key
  end
%>